{
  "id": "pol-deny-blocked-read",
  "description": "Deny READ to any principal whose roles include 'blocked'. Deny policies override allow policies in bouncer-engine, so a blocked principal is rejected even if they also hold a staff role.",
  "access": "DENY",
  "target": {
    "resource_type": "httpbin",
    "action": "READ"
  },
  "conditions": [
    {
      "attribute": "principal.role",
      "operator": "CONTAINS_ANY",
      "value": ["blocked"]
    }
  ]
}
